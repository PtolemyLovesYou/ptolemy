query getEventOutput($Id: ID) {
    systemEvents(filters: {id: $Id}) {
        outputs {
            ... on Output {
                id
                fieldName
                fieldValue
            }
        }
    }
}
