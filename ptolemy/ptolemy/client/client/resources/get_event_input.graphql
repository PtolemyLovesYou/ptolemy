query getEventInput($Id: ID) {
    systemEvents(filters: {id: $Id}) {
        inputs {
            ... on Input {
                id
                fieldName
                fieldValue
            }
        }
    }
}
