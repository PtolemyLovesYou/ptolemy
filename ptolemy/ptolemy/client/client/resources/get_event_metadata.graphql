query getEventMetadata($Id: ID) {
    systemEvents(filters: {id: $Id}) {
        metadata {
            ... on Metadata {
                id
                fieldName
                fieldValue
            }
        }
    }
}
